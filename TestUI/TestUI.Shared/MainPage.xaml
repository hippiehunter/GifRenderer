<Page
    x:Class="TestUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestUI"
    xmlns:gif="using:GifRenderer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
    x:Name="thePage">

    <Grid>
        <Grid.Resources>
            <gif:GifConverter x:Name="gifConverter"/>
        </Grid.Resources>
        <TextBox x:Name="theUrl" Text="http://i.imgur.com/UK7pvae.gif"/>
        <Button Click="Button_Click" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="100" Height="40" Content="The Button"/>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image DataContext="{Binding data, Converter={StaticResource gifConverter}}" Source="{Binding ImageSource}" Width="400" Height="230"/>
        </StackPanel>
        
    </Grid>
</Page>